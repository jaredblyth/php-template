$(document).ready(function() {

	$('#slideshow img').css("display","inline"); // Removes css class that is hiding images during load - if they are visible before document loads then all images tend to show until the scripts take effect - this is not desirable.

});
